1. This application is a game thats like a map of a school giving us directions on where to go.
* the commands the following game accepts are, "help" "go" "quit" "i dont know what that means"
* what each command does is revolved of what the commands name is for example using the command help you
will get a comment responding to your issue.
* there is 5 rooms, outside, theatre, pub, computing lab, office.
* the map would look like    
            outside
    theatre   lab    pub
            [office]

2. the game class makes the rooms and exits for the code.
the command class makes the the return of the commands you put for class game.
the parser class interprets the commands given in the terminal.
commandwords class is to confirm what the command words are if its not a command word it will 
return false.the class room shows where all the exits are in each room.

question 9. KeySet() method is part of the hashMap class. its used to get all the keys stored in the hashMap.